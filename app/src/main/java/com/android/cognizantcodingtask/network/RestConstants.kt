package com.android.cognizantcodingtask.network

class RestConstants {
    companion object {
        const val KEY_ACCEPT_ENCODING = "Accept-Encoding"
        const val KEY_USER_AGENT = "user-agent"
        const val KEY_DEVICE_ID = "device_id"

        const val KEY_PLATFORM = "platform"
        const val KEY_VERSION = "version"
        const val KEY_MODEL = "model"
        const val KEY_OS_VERSION = "os_version"

        const val VALUE_ANDROID = "android"
        const val VALUE_PAYLOAD = "Payload- "
        const val NETWORK_REQUEST_PAYLOAD = "NETWORK REQUEST :-"
        const val NETWORK_RESPONSE_PAYLOAD = "NETWORK RESPONSE :-"
        const val VALUE_IDENTITY = "identity"


    }
}